@{
    ViewData["Title"] = "Client Master";
}
<div class="container py-4">
    <h2 class="mb-3">Client Master</h2>
    <div class="alert alert-info">
        หน้าเปล่าสำหรับ Client (ยังไม่แสดงข้อมูล)
    </div>
</div>